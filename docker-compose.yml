volumes:
  postgres_data:
    driver: local
services:
  api:
    container_name: backend
    build: 
      context: ./api
    ports:
      - "8000:8000"
    networks:
      - admin-js
  client:
    build: 
      context: ./client
    ports:
      - "3000:3000"
    networks:
      - admin-js
networks:
  admin-js:
    driver: bridge